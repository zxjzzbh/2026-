# 小型测试样例

这里尚无实际样例。添加样例时同时记录：来源/许可、拍摄条件、图像尺寸、标注含义、预期用途和相关测试命令。

建议按任务命名，如 `lane/`、`board/`、`crosswalk/`、`traffic_light/`、`cones/`、`parking/`。只有实际存在素材时再创建子目录。

负样例和失败样例同样重要。不要只留下算法识别成功的画面，也不要把人工生成图片当作实车验证证据。
