# 集成与回归验证

存放跨模块验证；单模块测试放各模块的 `tests/`。目前没有测试代码和统一测试命令。

优先建立可离线重现的场景：视觉无效/断帧、模式切换、重复任务事件、过期运动命令、通信掉线、串口异常。输入尽量使用模拟消息和少量公开样例。

测试说明必须区分软件回放、硬件台架和实车，不把“脚本退出码为 0”写成整车通过比赛。实车结果写入 [测试记录](../docs/testing/README.md)。
