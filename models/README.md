# 模型索引与部署说明

当前未选择或部署模型。传统视觉基线不足时再根据实测决定是否引入模型。

模型权重放本地 `weights/`（已忽略），版本和来源登记 [catalog.csv](catalog.csv)。每个模型还需说明训练代码、数据版本、许可证、输入尺寸/颜色顺序/归一化、输出类别与含义、导出命令和推理依赖。

发布性能结果时注明硬件、线程、输入尺寸、批大小、精度和耗时。不能把其他平台或前辈报告的性能写成 Pi 4B 实测性能。
